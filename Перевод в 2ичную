#include <iostream>
#include<vector>
using namespace std;

int main()
{
	int a;
	cin >> a;
	vector<int> vec;
	while (a > 0)
	{
		vec.push_back(a % 2);
		a = a / 2;
	}
	for (int i = vec.size()-1; i >= 0; --i)
		cout << vec[i];
	return 0;
}
